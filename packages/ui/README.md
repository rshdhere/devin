# `@devin/ui`

Shared React UI primitives for the web app (button, card, code).

```ts
import { Button } from "@devin/ui/button";
```

Keep components small and presentational; app-specific layout lives in `apps/web`.
