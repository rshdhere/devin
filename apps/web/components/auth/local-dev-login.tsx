"use client";

import type { ReactNode } from "react";

/** Committed stub — staging/prod never ship the local-dev button. */
export function LocalDevLoginSlot(_props: { disabled?: boolean }): ReactNode {
  return null;
}
